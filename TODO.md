# TODO

Оперативный список. Стратегия — в [ROADMAP.md](ROADMAP.md), статус — в
[PROJECT_STATE.md](PROJECT_STATE.md). Весь P0 и релизы v1.0.0/v1.1.0 выполнены.

## Аудит v1.2 — выполнено

- [x] Даты истории/карточки в локальной зоне (были UTC).
- [x] Быстрые действия кликаются целиком (тап по подписи не работал).
- [x] Поле поиска не стирает ввод (управлялось debounce-значением).
- [x] Отмена доступна после отказа от «в список покупок?».
- [x] Информативный undo-снекбар, метка «Осталось», contentDescription для иконок.
- [x] User-Agent каталога из версии приложения; защита undo от «отката отката».
- [x] Редактирование продукта/партии (название, бренд, категория, срок, заметка).
- [x] Показ фото товара (карточка + миниатюра в списке).
- [x] Управление местами хранения (добавить/переименовать/основное/скрыть).
- [x] EN-локализация (values-en, plurals для уведомлений).
- [x] Compose-UI-тесты: 9 сценариев из ТЗ.
- [x] detekt + ktlint (spotless) + шаги в CI.
- [x] Пагинация истории (страницы по 100).

## Дальше (P1)

- [ ] Создать собственный keystore и перейти на него (конфигурация сборки готова,
      инструкция — в README «Подпись релиза»; ключ создаёт владелец).
- [ ] Совместный доступ, этап 2 (вариант B из [ADR-0004](docs/adr/0004-household-sharing.md)):
      обмен по локальной сети поверх той же механики журнала. Нужен, только если
      задержка облачной папки окажется неудобной на практике.

## Сделано после v1.3.0

- [x] Baseline Profile + макробенчмарк запуска ([docs/BASELINE_PROFILE.md](docs/BASELINE_PROFILE.md)).
- [x] Поиск по всем базам Open*Facts (`product_type=all`) — см. [docs/CATALOG.md](docs/CATALOG.md).
- [x] Цепочка провайдеров каталога: «нет товара» и «нет связи» больше не путаются.
- [x] Отправка неизвестных товаров в Open Food Facts (по аккаунту, с подтверждением).
- [x] Настройки в бэкапе (схема v2), подтверждение для «выбросить».
- [x] Единые токены (`Dimens`, `Shapes`), `ScreenScaffold` и `SectionCard` — на **всех**
      экранах, включая сканер, OCR и покупки.
- [x] **Совместный доступ, этап 1**: миграция схемы БД v1→v2 (uuid + deviceId),
      обмен журналами через общую папку, раздел в настройках, периодический обмен.
- [x] Партии больше не дублируются при повторном импорте (следствие uuid).

## Сделано после v1.5.0

- [x] Напоминания учитывают срок после вскрытия (`observeExpiringBefore` смотрел только
      на печатную дату — вскрытая пачка не попадала ни на главную, ни в виджет, ни в
      уведомление).
- [x] Быстрые действия долгим нажатием на строке списка + отмена на главной, в запасах
      и в покупках; «Ещё одна упаковка».
- [x] Произвольная дата при добавлении (общий `ExpiryPicker`), подсветка пресетов.
- [x] Плитка «Просрочено» на главной, фильтры состояния в запасах, метка «вскрыто».
- [x] Кнопки −/+ у количества; единицы измерения и правка позиций в списке покупок.
- [x] Вибрация при удачном скане, снекбары озвучиваются TalkBack.

## Технический долг

- [x] `SyncManager` покрыт `SyncManagerTest` (Robolectric, обычный каталог вместо SAF
      через `SyncFolderResolver`). Остаётся непокрытым поведение настоящего облачного
      клиента — наполовину записанный файл и конфликтные копии; это проверяется только
      на двух реальных телефонах.

- [ ] Обмен через облачную папку не мгновенный; о задержке сказано в интерфейсе.
- [ ] Удаление продукта у соседа не распространяется (партии помечаются удалёнными,
      сам продукт остаётся) — заметно только при чистке каталога.
